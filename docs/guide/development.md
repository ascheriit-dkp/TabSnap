# Development

Requirements:

- Node 24+
- pnpm 11+

```bash
pnpm install
pnpm check
pnpm docs:dev
```

## Done means done

A change is done when code, tests and docs agree.

Don't merge known broken CI.
