name: Format M34

on:
  push:
    branches:
      - feat/level4-hardening
    paths:
      - .github/workflows/format-m34.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: feat/level4-hardening
      - name: Rust toolchain
        run: |
          rustup update stable
          rustup default stable
          rustup component add rustfmt
      - run: cargo fmt --manifest-path apps/windows-companion/Cargo.toml
      - name: Commit formatting
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/windows-companion/src/capture.rs apps/windows-companion/src/restore.rs apps/windows-companion/src/machine.rs apps/windows-companion/src/protocol.rs
          git diff --cached --quiet || git commit -m "style: format M34 hardening tests"
          git push origin HEAD:feat/level4-hardening
