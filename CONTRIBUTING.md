# Contributing

Keep changes small.

Before opening a PR:

```bash
pnpm install
pnpm check
```

A change is done when code, tests and docs agree.

Use clear commit messages. Don't mix unrelated changes.

Security issues do not belong in public issues. Read `SECURITY.md`.
